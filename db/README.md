# Database Notes

Use this folder for schema exports, inspected table/column notes, migration planning, and query review artifacts.

The project specification references an existing PostgreSQL database:

- Server: `ssvcpro200`
- Database: `tryoutspot_prod`
- Expected table count from specification: 19

Before adding EF Core entities or migrations, inspect the live schema and record the findings here.
